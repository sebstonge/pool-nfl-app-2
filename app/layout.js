export const metadata = {
  title: "Pool NFL",
  description: "Application de pool NFL",
};

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  );
}
